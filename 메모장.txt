git add -A
git commit -m "second commit"
git pull origin main
git push origin main

패키지
pip install 패키지명
pip install --upgrade 패키지명
python -m pip install --upgrade pip
Ctrl + / 전체 주석
전체 코드 선택 ctrl + a
자동정렬 ctrl + shift + b

기본 자바스크립트 세팅 - rfce
서버 실행 - 경로지정한후 npm start
import ~ from 간단키 - imp
import {} from 간단키 - imd